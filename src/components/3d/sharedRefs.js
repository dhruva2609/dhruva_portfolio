/**
 * Module-level shared refs for cross-component communication
 * inside the R3F Canvas (no React context needed).
 */
export const hoverState = {
  active: false,
  targetBloomIntensity: 0.25,   // matches Bloom default
};
